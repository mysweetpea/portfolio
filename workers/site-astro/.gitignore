# Build output — regenerated by `npm run build`; never commit (the live tree is
# deployed from dist/, but it is a build artifact, not source).
dist/

# Dependencies
node_modules/

# Astro/Vite caches
.astro/
.vite/

# Local env
.env
.env.*
!.env.example
