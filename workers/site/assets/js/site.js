
(()=>{'use strict';const reduce=matchMedia('(prefers-reduced-motion: reduce)'),fine=matchMedia('(hover:hover) and (pointer:fine)');const progress=document.querySelector('.progress'),nav=document.querySelector('.nav');let ticking=false;function scroll(){if(ticking)return;ticking=true;requestAnimationFrame(()=>{const max=document.documentElement.scrollHeight-innerHeight; if(progress)progress.style.width=(max>0?scrollY/max*100:0)+'%';if(nav)nav.classList.toggle('scrolled',scrollY>28);ticking=false})}addEventListener('scroll',scroll,{passive:true});scroll();const toggle=document.querySelector('.menu'),links=document.querySelector('.nav-links');if(toggle&&links){toggle.addEventListener('click',()=>{const open=links.classList.toggle('open');toggle.setAttribute('aria-expanded',open)});links.addEventListener('click',e=>{if(e.target.closest('a')){links.classList.remove('open');toggle.setAttribute('aria-expanded','false')}})}const reveal=[...document.querySelectorAll('.reveal')];if(reduce.matches||!('IntersectionObserver'in window))reveal.forEach(e=>e.classList.add('visible'));else new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');e.target.classList.add('visible');}}),{threshold:.12}).observe&&reveal.forEach(el=>new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12}).observe(el));if(fine.matches&&!reduce.matches)document.querySelectorAll('.card').forEach(card=>card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--x',e.clientX-r.left+'px');card.style.setProperty('--y',e.clientY-r.top+'px')},{passive:true}));document.querySelectorAll('[data-count]').forEach(el=>{const target=+el.dataset.count,suffix=el.dataset.suffix||'';const run=()=>{if(reduce.matches){el.textContent=target+suffix;return}const start=performance.now(),duration=1100;const tick=t=>{const p=Math.min(1,(t-start)/duration),v=Math.round(target*(1-Math.pow(1-p,3)));el.textContent=v+suffix;if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)};if('IntersectionObserver'in window)new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){run();e.target.__obs?.disconnect()}}),{threshold:.6}).observe(el);else run()});})();
