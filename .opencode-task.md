# TASK: Aurora Polish — brand click, compact service orbs, security tab redesign, prettier hamburger (SPA-only; single file)

Four refinements to `workers/dashboard/public/index.html`. All verified working today; do not
regress any of it (mobile passes 1-9 must stay intact).

## 1. Brand = favicon → dashboard Home
- Current: `<a class="brand" href="https://mysweetpea.cc" title="Back to mysweetpea.cc"><img src="/icons/logo.svg"/> <span class="back-label">&larr; Back to site</span></a>`
- Change to: `<a class="brand" href="#" data-goto="home" title="MySweetPea"><img src="/icons/logo.svg"/> <span class="back-label">MySweetPea</span></a>`
  - The label text becomes **MySweetPea** (not "Back to site"), still the logo.svg favicon image
  - Clicking it must go to the dashboard **Home tab** (use the existing pattern: the brand is in
    the nav template string; add the same delegated behavior the other data-goto targets use —
    find how `[data-goto]` clicks are handled and make brand work with it, OR onclick="setTab('home')").
    It must NOT navigate to mysweetpea.cc anymore.
  - Keep the `.site-back` "Return to Website" button (↗) as the way out to the main site — unchanged.
- Adjust `.back-label` CSS if needed so "MySweetPea" fits nicely on mobile (Fraunces, ~0.95rem is fine).

## 2. Home services = compact orbs instead of wide tiles
- The Home "Your services" rail renders service cards with big icon tile + name + blurb (find the
  builder around `hmLoadServices` / `hm-acct-rail`… the SERVICES grid used on the Home tab; it
  currently produces tile-like cards ~230-300px wide with `.hm-orb`/tile classes).
- Make a COMPACT variant on mobile (inside a new `@media (max-width:760px)` block labeled
  `/* ---- aurora mobile 10: compact services ---- */` at the end of the stylesheet):
  - grid auto-fill minmax(0, 96px), gap 10px; cards become vertical chips: 40px icon, 11px name,
    NO blurb on mobile (`.hm-orb-bl{display:none}`), padding 10px 8px, center-aligned
  - status dot shrinks to 7px and pins top-right of the icon
  - card stays tappable (44px min height)
- Desktop appearance must NOT change.

## 3. Security tab: friendlier checklist
- Current render (in JS, around the `sec.innerHTML = '<p class="eyebrow">Security</p>...` build):
  ring % + a plain `.check` list. Redesign the markup it emits to be warmer and clearer:
  - Big friendly title: **"Your account's protective circle"** with a one-line sub:
    *"A few quick steps and your account is locked down tight."*
  - Keep the SVG progress ring but enlarge to 64px and put the % inside with Fraunces font
  - Each checklist item becomes a card row: left = big status icon (green ✓ in a soft green
    circle when done, a soft violet ● number/pending glyph when pending), middle = step name
    (semibold) + a plain-language description of WHY it matters (one short line, e.g.
    "Passkeys — sign in with your face or fingerprint. Phishing-proof."), right = an action
    button ("Set up" / "Manage") using existing `.ob-btn` styling that triggers the SAME
    ceremony() calls already wired in items[]
  - Completed items get a subtle green tint background; the section gets a completion banner
    line when 100%: "🌱 Your circle is complete — your account is fully protected."
  - Keep ALL existing behavior: items[] logic, done detection, ceremony wiring, aria labels.
- Add the small CSS for these rows (`.sec2-row` etc.) in a labeled block; keep it compact.

## 4. Hamburger button + open menu: prettier
- The `.nav-toggle` is a 38px square with 3 bars. Polish: 42px, rounded-xl (12px), soft violet
  glass background `rgba(151,146,255,.10)`, 1px violet border like `.site-back`, bars 18px wide
  2px tall with 5px gap, and animate to an X when `.nav-open` (bars 1&3 rotate ±45° and translate,
  middle fades) — the class hook `nav.nav-open` already exists in initNavShell.
- The mobile dropdown (`.nav-tabs` panel at ≤760px): give the open panel a gentle entrance
  animation (opacity + translateY(-8px) → 0, 0.28s, the same ease as the site's mobileNavOpen),
  rounder corners (16px), softer shadow, and each `.tab` row gets a leading icon dot (6px, violet)
  with the active row's dot turning gradient violet→ice.

## HARD CONSTRAINTS
- ONLY edit `workers/dashboard/public/index.html`
- New CSS goes in labeled blocks at the end of `<style>`; JS edits limited to: brand anchor
  attributes, the security sec.innerHTML template, and (only if needed) one small delegated
  click handler for the brand. Do not touch worker src, do not touch acct-* dropdown, do not
  break mobile passes 1-9 (minmax(0,1fr) grids etc.)
- Never write outside the repo; scratch only in `.oc-tmp/`
- Verify: node --check on both inline script blocks, brace balance 0, grep confirm:
  brand href="#"/data-goto or setTab('home'), "protective circle" present, `.hm-orb-bl{display:none}`
  present, `nav-open` keyframes present; confirm NO reference to mysweetpea.cc in the brand <a>
- Write `.oc-tmp/aurora-polish-verification.md`, print READY TO DEPLOY
