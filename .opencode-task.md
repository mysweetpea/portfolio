# TASK: Aurora Polish 4 — six fixes (SPA-only; single file + tiny worker addition)

## FIX 1 — "Up next for you" cards show NO artwork on iPhone (ROOT CAUSE FOUND)
`hmUnCard()` ignores the API-provided image and calls `hmBackdrop(it.id, 480)` →
`/Items/{id}/Images/Backdrop` which 404s for most /Items/Latest entries (they only have
Primary images). The API ALREADY returns a working `img` field (Primary image URL).
CHANGE in `hmUnCard()`: use `(it && it.img)` for the art src, fallback to hmBackdrop:
  'src="' + esc((it && it.img) || hmBackdrop(it && it.id, 480)) + '"'
Also add an `onerror` fallback to the backdrop variant (if primary 404s, try backdrop once):
  onerror="if(!this.dataset.f){this.dataset.f=1;this.src='...backdrop url...'}else{this.style.display='none'}"
(follow the existing onerror pattern used in hmWideCard).
The static-flow meta from polish-3 stays as-is.

## FIX 2 — Spotlight: progress-slider timer, show ALL services, remove → next arrow
Current: `.sv-spot` cycles via ↻ refresh + `.sv-next` arrow every 8s (hidden interval).
CHANGE to a progress-slider pattern (user requirement):
- REMOVE the `.sv-next` arrow button entirely (markup + any JS referencing it).
- ADD a thin progress bar along the spotlight's bottom edge (like the story-ring pattern):
  `<span class="sv-spot-timer"><i></i></span>` inside `.sv-spot`, full width, 3px tall,
  violet→ice gradient fill that animates from 0→100% width over the 8s cycle
  (CSS transition/animation restarted each cycle by re-adding a class or setting width via JS rAF;
  simplest: an <i> whose width is set by the same interval using performance.now() deltas, or a
  CSS animation `sv-timer 8s linear` re-triggered by removing/adding the class).
- SHOW ALL 9 services, not a subset: check `SV_FEATURED` array — it currently contains only a
  few names (Jellyfin, Seerr, Nextcloud, Immich, Open WebUI). Replace with ALL SERVICES names
  (build it dynamically: `let SV_FEATURED = SERVICES.map(s => s.name);` at the same spot, keeping
  the pinned/favorites OUT unless already there). Order = SERVICES order.
- Keep the ↻ refresh button (manual advance + timer reset). Remove any `.sv-next` handler.
- Keep reduced-motion pause + pause when tab hidden.

## FIX 3 — Statistics page: add real charts
The stats tab currently has KPI tiles + text. Add lightweight charts (NO external libs — hand-
rolled SVG/CSS, single file constraint):
1. **Library composition bar**: one horizontal stacked bar (Movies / Series episodes / Photos)
   with legend, from /api/stats numbers (movies, episodes, photos). Pure CSS flex widths.
2. **"Where your time goes" donut**: SVG donut (stroke-dasharray circles) splitting
   jf_watch_hours vs photos vs (total-photos) — OR simpler: donut of Movies vs Series vs Boxsets
   counts. Pick whichever fields exist in /api/stats; handle nulls by hiding that segment.
3. **Media vs Photos KPI mini-sparklines**: two tiny inline SVG sparklines are NOT possible
   without history — SKIP sparklines; instead add a **"by the numbers" comparison row**:
   horizontal bar pairs (e.g., Movies 204 ▮▮▮ vs Series 38 ▮) normalized to the max value.
   Pure CSS.
Place them in the stats panel above the existing per-service sections, inside a new labeled
section `sec-stats-charts`. Add a small `<noscript>`-safe empty state. All colors from existing
CSS vars (--v, --c, --sage, --gold). Charts must update when stats refresh button is pressed
(observe how /api/stats response flows into the KPI render and hook the same spot).

## FIX 4 — Sessions: use the CORRECT Authentik API fields (research completed)
Authentik 2026.8 `authenticated_sessions` serializer ACTUALLY returns:
  uuid, current (bool), user (obj), last_ip, last_user_agent (RAW string),
  last_used (datetime), expires (datetime),
  user_agent: { user_agent:{family,major}, os:{family}, device:{family,brand,model} },
  geo_ip (nullable), asn (nullable)
Our SPA wrongly reads s.current_session / s.last_seen / s.agent — fix EVERY usage:
- `s.current_session` → `s.current`
- `s.last_seen` → `s.last_used`
- `uaString(s)` → prefer `s.user_agent && s.user_agent.string` (parsed dict has .string),
  fallback `s.last_user_agent`, then the old s.agent shape
- Row secondary line: `last used · IP` via `relTime(s.last_used) || s.last_used` + `s.last_ip`
- Build a friendly device label from the PARSED user_agent dict:
  browser family (Safari/Firefox/Chrome…) + os family (iOS/Windows/Android…) → "Safari on iOS".
  Fallback to old devLabel if dict missing.
- Add expiry hint: if `s.expires` exists, append muted " · expires <relTime>" to the secondary line.
- geo_ip/asn: if present and non-null, show as a third muted line "City, Country · ASN name";
  hide when null. (authentik geoip may be unlicensed → expect null, handle silently.)
- Keep: this-device green pill uses s.current; per-row Sign out uses s.uuid; Explainer line stays.

## FIX 5 — hm-un fallback art already covered in FIX 1 (same file)
No extra work — listed here for completeness of the ghost-band saga.

## FIX 6 — worker: extend /api/stats with watch-hours history (SMALL, optional)
SKIP unless trivial: do NOT change /api/stats beyond what exists (jf_watch_hours already used
by the wrapped hero). Charts in FIX 3 use existing fields only.

## HARD CONSTRAINTS
- Edit ONLY `workers/dashboard/public/index.html` (worker src untouched)
- CSS in labeled blocks at end of <style> (`aurora polish 4`); JS edits limited to hmUnCard,
  spotlight timer/SV_FEATURED/sv-next removal, stats charts section + its render hook, sessions
  renderer field mappings
- Do not break: mobile passes 1-10, brand→Home, security circle block, request ETA chip
- Verify: node --check both script blocks, brace balance 0, grep: no `.sv-next` remains,
  `SV_FEATURED` built from SERVICES, `s.current` used, `s.last_used` used, charts section present
- Write `.oc-tmp/aurora-polish4-verification.md`, print READY TO DEPLOY
