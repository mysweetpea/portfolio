# TASK: Aurora Mobile 6 — Home tab fit on phones (SPA-only; single file, CSS-only)

Two iPhone screenshots show the HOME tab panned sideways / clipped on BOTH edges: greeting
"Good evening, authentik Default Adr|" cut right, email "dmin@..." cut left, stats strip cut,
purple CTA reduced to a sliver, the Now-Watching hero art clipped both sides and its play
button half off-screen, "Trending" heading cut to "…ing", first trending poster clipped left
with right half empty, "…Services" heading cut. The nav bar is fine. iOS Safari, ~390px.

Root causes to fix (verify each in the file first):
1. `.hm-greet` at 1.65rem base with `white-space` unset can still push `.hm-id` (min-width:210px
   base, mobile sets min-width:0) — but the LONG username "authentik Default Admin" has no
   break opportunity: add `overflow-wrap:anywhere;min-width:0` on `.hm-greet` and `.hm-id` mobile.
2. `.hm-stats` uses `justify-content:space-between` with 3 `.hm-stat` items that have
   `letter-spacing` + padding; if total > width the row overflows the banner. Make it
   `display:grid;grid-template-columns:repeat(3,auto);justify-content:space-between;gap:4px`
   and `.hm-stat{padding:4px 6px;font-size:.62rem}` on mobile. `.hm-stat .n` numbers: cap
   `font-size:1.05rem`.
3. `.hm-actions` buttons: the primary CTA label ("Open..."/"Security · 1 step left") is long —
   on mobile make `.hm-actions .hm-btn{flex:1 1 auto;min-width:0}` so both buttons share the row
   and text ellipses instead of pushing the row wide. Add `overflow:hidden;text-overflow:ellipsis;
   white-space:nowrap` on `.hm-btn`.
4. `.hm-hero-inner` (absolute, left:16 right:16 mobile) — the play button inside it is being cut
   because `.hm-hero-copy` has no min-width and the button row doesn't wrap: on mobile make
   `.hm-hero-inner{flex-direction:column;align-items:stretch;gap:10px}` and
   `.hm-hero-actions{flex-wrap:wrap}` (find the actual class of the play button row in the markup
   around `hm-hero-actions`/`hm-play` and apply min-width:0 + flex-wrap there). The hero
   `<img class="hm-hero-art">` is inset:0 width:100% — fine once the page stops being pushed wide
   by elements above.
5. Trending row: `.hm-rail:has(.hm-pcard)` 2-col rule — REPLACE the :has() dependency (user's
   Safari doesn't apply it): directly set `.hm-rail{display:grid;grid-template-columns:repeat(2,1fr);gap:14px}`
   inside the 760px block (keep overflow:visible), `.hm-rail>*{width:auto!important}` (NOT 100%,
   which over-floors), `.hm-poster` keeps aspect-ratio and fills the card. Also `.hm-pcard .mtitle`
   needs the 2-line clamp like .media-row.
6. The `.hm-sec::after` fade hint is already display:none on mobile — keep.
7. Guard: `.hm-banner-row{flex-wrap:wrap}` so nothing in the banner can force width.
8. `.hm-h2`/`.card h3` are fine; make sure `.hm-sec` has `min-width:0` and `.grid` (main) has
   `min-width:0` so grid children can't expand it.

Add ONE new media block at the very END of the stylesheet (after aurora mobile 5) labeled
`/* ---- aurora mobile 6: home fit ---- */` so it wins the cascade. CSS-only. 760px breakpoint.

## HARD CONSTRAINTS (violating any = failed task)
- ONLY edit `workers/dashboard/public/index.html`
- CSS ONLY — do not touch any JavaScript, do not touch the worker src
- All new rules inside ONE new `@media (max-width:760px)` block at the end of the <style>
- NEVER write outside the repo dir; scratch files only in `.oc-tmp/`
- Do not modify: `acct-*` dropdown rules, `.site-back`, tab styles, footer styles
- Verify after editing: (1) `node --check` passes on both inline script blocks — extract them
  exactly as in the file; (2) brace balance delta 0 for the whole file; (3) diff contains ONLY
  your one new CSS block; (4) grep confirm: `.hm-rail{display:grid` present, no `:has(` in your
  new block, `overflow-wrap:anywhere` present
- Write findings + checks to `.oc-tmp/aurora-mobile6-verification.md`, then print READY TO DEPLOY
