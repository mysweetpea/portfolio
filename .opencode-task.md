# TASK: Aurora Mobile 2 — make every element FIT on phones (SPA-only; single file)

Follow-up to the overflow fix. The page no longer scrolls sideways (good), but now some
elements get CLIPPED or CRUSHED on phones instead of fitting. Diagnose confirmed:

## Confirmed problems to fix (all inside @media max-width:760px unless noted)

1. **Stats KPI grid crushed**: `.stats{grid-template-columns:repeat(4,1fr)}` has NO mobile
   override -> 4 tiles ~80px wide on a 375px phone. FIX: <=760px -> `repeat(2,1fr)`.
2. **Guides two-column stays**: `.svc-guide{grid-template-columns:220px 1fr}` never collapses
   -> 220px chip column + crushed detail on phones. FIX: <=760px -> `1fr` single column,
   `.svc-guide-nav` becomes horizontal wrap row of chips (flex-wrap:wrap, flex-direction:row).
3. **Card padding too big on phones**: `.card{padding:40px 44px}` never shrinks. FIX:
   <=760px -> `.card{padding:20px 16px}`.
4. **Shell margins**: `.shell{width:min(1080px,92vw)}` -> <=760px use `width:auto;
   margin:0 10px` so content spans nearly full width.
5. **Rail discoverability ("cut off" feeling)**: cards in `.hm-rail`/`.sv-orbs` are 210-300px
   fixed so ~1 card fits per screen. KEEP the rail pattern (correct on mobile) but:
   a) slim the cards <=760px: `.hm-req{width:220px}` `.hm-wide{width:200px}` `.hm-un{width:180px}`
      `.hm-tile{width:230px}` `.hm-orb{width:120px}` `.hm-pcard{width:112px}`
   b) add a right-edge fade hint on every rail so half-visible next card is obvious:
      `.hm-sec{position:relative}` + `.hm-sec::after{content:'';position:absolute;right:0;top:0;
      bottom:0;width:28px;background:linear-gradient(90deg,transparent,var(--bg));pointer-events:none}`
      (apply the same to the services orbs container wrapper and media rows if they have a
      section wrapper; skip where a wrapper doesn't exist).
   c) first rail card should align with content edge (padding-left:2px already ok).
6. **Uptime rows / library rows**: verify no fixed width overflows; `.lib-l` 78px + `.lib-n` 70px
   are fine — leave.
7. **Footer**: already stacks 2x2 — leave.
8. **acct-pop (account dropdown)**: 230px wide, opens right-aligned — verify it can't overflow
   left edge on 320px; if needed add `right:0` positioning guarantee at <=760px.
9. **Top nav <=760px**: hamburger menu exists; verify site-back icon + search(hidden <=760?)
   + account fit in 56px height — shrink gaps if crowded, no functional change.

## HARD CONSTRAINTS

- ALL changes inside `@media (max-width:760px)` blocks (or the existing 761-1080 block) —
  desktop >=1081px must remain pixel-identical.
- Do NOT touch: worker src, JS logic, auth, routes, tokens, body ambience, footer status.
- May add classes only where a wrapper is missing for the fade hint (prefix `mb2-`).
- One file; scratch only `.oc-tmp/`; no git/wrangler.

## Verification

node --check every script block; brace balance; grep-proof that ALL new rules sit inside
media queries; protected markers (acct-pop, hm-hero, sv-spot, st2-num, ct2-mail, PLATFORMS);
write `.oc-tmp/aurora-mobile2-verification.md`; print READY TO DEPLOY.
