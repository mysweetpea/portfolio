# TASK: Aurora Phase 3b-2 — Statistics wrapped hero (SPA-only; single file, SMALL scope)

Add a **"This Week" wrapped hero** to the TOP of the STATS tab in
`workers/dashboard/public/index.html`. Mockup reference: `.oc-tmp/mockups/stats.html`
(adapt the hero look; do NOT port the mockup's static data).

## Build

1. Above the existing stats KPI section inside `panel-stats`, add a hero section (class `st2-hero`):
   - Eyebrow: "This week"
   - Big Fraunces gradient number line: `<hours> hrs <mins> min` (gradient var(--primary) -> var(--sage),
     background-clip:text, clamp(2.2rem, 5vw, 3.4rem))
   - One narrative sub-line: "across <N> titles on <M> devices — <top title> claimed your biggest sitting"
     using the same data with graceful fallbacks ("—" when a value is null)
   - Small period pill row (Week / Month / All time) — visual only for now; Week active by default;
     clicking switches active styling only
2. Data: fetch `/api/stats` ONCE and reuse — the stats tab ALREADY fetches `/api/stats` for its KPIs.
   Find that fetch and extend it (or memoize with a module-level promise) so the hero reads from the
   same payload. Fields available: the stats handler returns movies/series/episodes/photos/etc —
   it has NO watch-hours field. Add `jf_watch_hours` to the worker's /api/stats response:
   in `workers/dashboard/src/index.ts`, inside the stats handler's Promise.all, query Jellyfin
   `/Items?userId=' + env.JELLYFIN_USER_ID + '&Recursive=true&Filters=IsResumable&Fields=UserData,PlaybackPositionTicks&Limit=50`
   (x-emby-token header, same as the existing resume-count call) and sum
   `item.UserData.PlaybackPositionTicks / 3.6e9` across items -> hours (1 decimal). Titles count =
   number of resumable items; top title = first item's Name. Devices: leave as null -> fallback dash.
   Add the field to the existing `out` object (jf_watch_hours, jf_resume_titles, jf_top_title).
   SPA reads `stats.jf_watch_hours` etc. Guard everything with try/catch -> null.
3. Empty state: if jf_watch_hours is null/0, hero shows "Your week starts with your first play." line
   instead of the number.

## HARD CONSTRAINTS

- Do NOT touch: nav, home hm-*, services sv-*, contact ct2-*, guides/security/account panels,
  media panel, tokens, body ambience, palette, footer, auth code.
- SPA: only add `st2-`-prefixed CSS (marked block at end of style) + the hero markup + a small
  `st2Render(stats)` function hooked into the EXISTING stats fetch path. Do not create a second
  /api/stats fetch.
- Worker: only add the 3 fields inside the existing stats handler Promise.all. Do not restructure.
- One file each; scratch only in .oc-tmp/; no git/wrangler.

## Verification

node --check SPA script blocks + `npx tsc --noEmit` is NOT available — instead run
`npx esbuild workers/dashboard/src/index.ts --bundle --outfile=.oc-tmp/bundle.js --external:*`
if esbuild exists (skip gracefully if not), else `node --check` after a naive TS-strip is fine.
Brace balance. Write `.oc-tmp/aurora-p3b2-verification.md`. Print READY TO DEPLOY.
