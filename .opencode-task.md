# TASK: Aurora Mobile 7 — fix crushed 'Up next' cards (SPA-only; single file, CSS-only, TINY)

BUG: On phones, between 'Keep watching' and 'Your account', the 'Up next for you' rail renders
as a ~50px-tall band: card titles garbled/overlapping, one squashed art sliver, EPISODE pill at
its edge. ROOT CAUSE (confirmed): `.hm-un` cards use aspect-ratio:16/9 with the artwork absolutely
positioned (inset:0); aurora-mobile-6's rule `.hm-rail>*{width:auto!important}` removed their
definite width, so the <a> collapses to content width and aspect-ratio computes a tiny height.

FIX (in a new block at the end of <style>, labeled `/* ---- aurora mobile 7: up-next width ---- */`,
inside `@media (max-width:760px){ ... }`):
```
.hm-rail>.hm-wide,.hm-rail>.hm-un{width:100%!important;aspect-ratio:16/9;min-width:0}
.hm-un .hm-wide-t{font-size:.86rem}
.hm-wide .hm-wide-t{font-size:.86rem}
```
(100% of the 2-col grid column — definite width, so aspect-ratio gives a proper 16:9 card.)

## HARD CONSTRAINTS
- ONLY edit workers/dashboard/public/index.html; CSS only; no JS changes
- One new labeled block at the very end of the stylesheet; do not alter earlier blocks
- Never write outside the repo; scratch in .oc-tmp/
- Verify: node --check both inline scripts, brace balance 0, diff = only your block,
  grep confirm `.hm-rail>.hm-wide,.hm-rail>.hm-un{width:100%!important` present
- Write `.oc-tmp/aurora-mobile7-verification.md`, print READY TO DEPLOY
