# TASK: Aurora Mobile 3 — adopt the main site's mobile grammar (SPA-only; single file)

The dashboard's mobile view must feel like mysweetpea.cc's mobile view: a calm, single-column,
vertically-scrolling page with big readable headings, full-width cards, generous spacing, and
44px+ touch targets. NOT a shrunken desktop mosaic, NOT horizontal rails with half-visible cards.

Reference values below are taken from the main site's real CSS (`workers/site/assets/css/site.css`,
its `@media (max-width:768px)` / `(max-width:640px)` blocks). Match their feel.

## Changes — ALL inside @media (max-width:760px) blocks (desktop >=1081px untouched)

### 1. Kill the rails on phones — stack them
- `.hm-rail, .sv-orbs { display:grid; grid-template-columns:1fr; overflow:visible; }`
- Their children become full-width: `.hm-rail > *, .sv-orbs > * { width:100% !important; }`
  (overrides the fixed 120-300px card widths; keep the !important scoped to this block)
- Remove the right-edge fade hints on phones (they only make sense for horizontal rails):
  `.hm-sec::after, .media-sec .card::after, .mb2-orbwrap::after { display:none; }`
- EXCEPTION — poster rails: cards that are pure posters (`hm-pcard`, `.mitem`/`.mposter` in the
  media tab) become a 2-column poster grid instead of stacked:
  `.hm-rail:has(.hm-pcard), .media-row:has(.mposter) { grid-template-columns:repeat(2,1fr); }
   .hm-pcard, .mitem, .mposter { width:auto; }` (use the :has() form; if :has support is a
  concern, ALSO add plain fallbacks: `.hm-pcard, .mitem, .mposter { width:auto; }` unconditionally
  in the block — posters in a 1fr grid just fill their cell).

### 2. Typography up — site-scale headings
- `.card h3, .hm-h2 { font-size:1.35rem; }`
- `.hm-hero-title { font-size:clamp(1.5rem,7vw,1.9rem); }`
- `.sv-spot-name { font-size:clamp(1.3rem,6vw,1.6rem); }`
- `.st2-hero` number: `font-size:clamp(2rem,9vw,2.8rem);`
- `.hm-greet` already clamps — bump to `clamp(1.25rem,6.5vw,1.5rem);`

### 3. Section rhythm — breathe like the site
- `.tabpanel { gap:26px; }`
- `.hm-sec { margin-bottom:6px; }`
- `.shell { padding-top:76px; }` (keep 10px side margins)

### 4. Touch targets — 44px minimum everywhere interactive
- `.tab { min-height:44px; display:flex; align-items:center; }` (in the mobile dropdown panel)
- `.hm-btn, .ob-btn, .btn, .sv-fchip, .sv-dot, .sv-refresh, .acct-trigger, .nav-search { min-height:44px; min-width:44px; }`
  (sv-dot/sv-refresh keep their visual size but expand the tap area via padding, not layout break)
- `.sv-pin { width:36px; height:36px; }` (it overlays cards; 36px + card edge is acceptable)
- `.gp-` platform pills and `.guide-variant` pills: `min-height:40px; padding:10px 16px;`

### 5. Services dock = single column, site-style
- `.sv-grid { grid-template-columns:1fr; }` (full-width service cards, exactly like the site's
  services page mobile)
- `.sv-spot { padding:22px 18px; }`

### 6. Stats + misc
- `.stats { grid-template-columns:repeat(2,1fr); }` (already — keep)
- `.stat { padding:18px; }`
- `.hm-tile, .hm-req, .hm-wide, .hm-un { width:auto; }` (redundant-safe with #1)

## HARD CONSTRAINTS
- CSS only, inside @media (max-width:760px) blocks. NO JS edits, NO worker changes, NO desktop
  rule changes (desktop >=1081px must remain pixel-identical).
- Do not touch tokens, body ambience, footer status JS, auth UI, acct-pop behavior (may resize).
- One file; scratch only in `.oc-tmp/`; no git/wrangler.

## Verification
1. node --check every script block (must be untouched — verify zero JS diff).
2. Brace balance; file ends </html>.
3. Grep-proof: every line you added sits inside a 760px media block (show the check).
4. Confirm the fade-hint rules now have display:none inside 760px block.
5. Protected markers present: acct-pop, hm-hero, sv-spot, st2-num, ct2-mail, PLATFORMS.
6. Write `.oc-tmp/aurora-mobile3-verification.md`; print READY TO DEPLOY.
