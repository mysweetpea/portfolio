# TASK: Aurora Phase 3b-3 — Guides platform picker (SPA-only; single file, SMALL scope)

Add a **platform layer** to the Guides tab in `workers/dashboard/public/index.html`.
Mockup reference: `.oc-tmp/mockups/guides.html` (adapt its platform pill row look only —
do NOT port its static step data; the REAL steps live in the existing GUIDES object).

## Context

The guides system is LIVE data-driven: `GUIDES` object (9 services) -> `GUIDE_ORDER` ->
`guideChipHtml(k, sel)` renders chips -> `guideDetailHtml(key)` renders the detail pane.
Jellyfin already has `variants` (Moonfin / Jellyfin Web). Read all of it first.

## Build

1. **Platform model**: add a `PLATFORMS` map (do NOT touch existing GUIDES structure) —
   per guide key, an array of platform entries:
   - jellyfin: [web (uses existing variants!), iphone, android, androidtv, appletv, roku, desktop]
   - seerr: [web, moonfin] (moonfin note: "Requests live inside the Moonfin app — no app needed")
   - nextcloud: [web, desktop, iphone, android]
   - immich: [web, iphone, android]
   - vaultwarden: [browser, iphone, android, desktop]
   - matrix: [web, iphone, android, desktop]
   - affine: [web, desktop]
   - openwebui: [web]
   - koalasync: [web, extension]
2. **Per-platform steps**: for guides where the existing `steps` array is generic, ADD a
   `platformSteps` map INSIDE the PLATFORMS structure (not in GUIDES) with 2-4 realistic steps
   per platform (device-store links, autofill setup, CalDAV etc — short, factual, with url/btn
   where a real official URL exists; empty string when none). Platform entry with no
   platformSteps falls back to the guide's generic steps.
3. **UI**: in `guideDetailHtml`, above the steps list, render a platform pill row
   (device glyphs via small inline SVG or emoji-free CSS shapes; label). Default selection =
   first platform. Keep the existing variant picker (Moonfin/Web) VISIBLE only when
   guide=jellyfin AND platform=web. Steps re-render on platform switch. Progress/checkmark
   state (if any exists) keys per platform (existing state shape + platform suffix).
4. **Card chips**: on the guide chips row, show a tiny platform count badge (e.g. "7") on the
   Jellyfin chip only if trivially cheap; skip if it complicates the existing chip code.
5. Mobile: pill row wraps; detail pane unchanged otherwise.

## HARD CONSTRAINTS

- Do NOT touch: nav, home hm-*, services sv-*, contact ct2-*, stats st2-*, media/security/
  account panels, tokens, body ambience, palette, footer, auth, the /api routes, worker src.
- Do NOT modify existing GUIDES entries (platform data lives in the new PLATFORMS map).
- New CSS prefixed `gp-` in a marked block at the END of the style block.
- Keep existing guide ids/handlers working (guide-nav, guide-body, chips flow).
- One file; scratch only .oc-tmp/; no git/wrangler.

## Verification

node --check every script block, brace balance, confirm `guideDetailHtml` still referenced,
write `.oc-tmp/aurora-p3b3-verification.md`, print READY TO DEPLOY.
