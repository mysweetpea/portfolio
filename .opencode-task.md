# TASK: Aurora Phase 3a — Services tab rebuild + Contact polish (SPA-only; single file)

Rebuild the **SERVICES tab** content of `workers/dashboard/public/index.html` to match the
approved mockup `.oc-tmp/mockups/services.html` (the "combined" concept: featured spotlight +
pinned orbs + recently-opened + dock grid), adapted to real data in the file.

## Context

- Single-file SPA. Phase 1 (Aurora tokens/nav) + Phase 2 (home `hm-*` sections) are live.
  Do not modify anything from those phases.
- `.oc-tmp/mockups/services.html` is the target design. Read it freely (inside repo).
- Existing data available in the file: `SERVICES` array (9 services w/ name/url/blurb/icon),
  `/api/status` fetch (monitor `current` booleans, names match service names mostly),
  `/api/status/extended` (uptime `up24` per monitor). Check exact shapes in the file.

## Build (services panel content, in the big template string + render JS)

1. **Featured spotlight** — full-width glass banner. Rotate through 3 hand-picked services
   (Jellyfin, Immich, Open WebUI) every 8s + dot indicators + manual refresh arrow button.
   Content: service icon, name, blurb, "Open" button, uptime% if available.
2. **Pinned row** — horizontal orb rail. State: `localStorage['msp-pinned-svcs']` (JSON array
   of service names, default: Jellyfin + Vaultwarden). Hover dock card -> star button -> pin;
   hover orb -> X -> unpin. Toast on change (reuse existing toast() helper).
3. **Recently opened** — chip row fed by `localStorage['msp-recent-svcs']` (update on every
   service click anywhere in the file — hook into the existing grid click handler + orbs).
   Show last 5, most recent first.
4. **Dock grid** — all 9 services as glass cards (icon, name, blurb, status dot, uptime%).
   Toolbar above: category filter chips (All / Media / Cloud & Files / Tools / AI) mapped to
   the 9 services sensibly; sort toggle Default <-> A-Z; card hover star to pin.
5. **Suggest-a-service tile** at grid end -> links to existing suggest flow (check how the
   current file links it; keep the same destination).

## Contact tab polish (small)

- Headline becomes "Talk to a human." with sub-line "No tickets, no bots pretending — a real
  person reads every email."
- Email as the hero: large Fraunces gradient text + two buttons (Write an email = mailto with
  subject prefill; Copy address with inline "Copied" state using existing clipboard helper).
- Keep the existing "Before you write" content below (light restyle only).

## HARD CONSTRAINTS

1. Do NOT touch: nav (`top-nav`/`site-back`/`acct-*`), home `hm-*` sections, media/stats/
   guides/security/account panels, palette, footer status, tokens, body ambience.
2. New CSS prefixed `sv-` (services) and `ct2-` (contact) in a marked section at END of style
   block. Aurora vars only (--v, --c, --v-soft, --c-soft, --card, --radius, --gold...).
3. New JS functions prefixed `sv`. Keep existing IDs (`svc-grid`, etc.) working or update
   render code coherently. SERVICES array itself must not change.
4. Every fetch in try/catch; sections hide gracefully when data missing.
5. localStorage guards (try/catch — private mode safe).
6. Rails horizontal-scroll + snap on mobile; banner stacks <=760px.
7. One file, doctype intact. Scratch only in `.oc-tmp/`. No git/wrangler commands.
8. XSS: service names/blurbs are static (fine), but any dynamic text must use the existing
   esc() helper.

## Verification (do all, then `.oc-tmp/aurora-p3a-verification.md`)

1. node --check every script block (extract to .oc-tmp/*.js first).
2. Brace balance whole file.
3. Route list unchanged (17 routes).
4. `acct-trigger`, `hm-hero`, `updateFooterStatus`, `site-back` still present.
5. Summary + line delta in the verification file. Print "READY TO DEPLOY".
