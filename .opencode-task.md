# TASK: Aurora Phase 2 — Home tab rebuild (SPA-only; single file)

Rewrite the **HOME TAB** content of the dashboard SPA at `workers/dashboard/public/index.html`
to match the approved mockup `.oc-tmp/mockups/home.html` (Home-11 "Everything" concept),
adapted to the REAL data sources and real component classes already in the file.

## Context

- The file is a single-file SPA: one `<style>` block (~40KB) + JS that builds a nav + tab panels
  via template strings and populates them from `/api/*` routes.
- Phase 1 already shipped the Aurora design system: tokens (--v violet #8B7EF6, --c ice #45D6C8,
  --v-soft/--c-soft, --bg #08090F), aurora body ambience, ghost-pill tabs, `.site-back` button.
  DO NOT change tokens, body ambience, nav structure, tabs, or the account dropdown (`acct-*`).
- Reference mockups are INSIDE the repo: `.oc-tmp/mockups/*.html` — read them freely.
  `home.html` is the primary target; `nav.html` shows the nav (already live, do not re-port).

## What to build (HOME tab = the `panel-home` content in the JS template)

Order top-to-bottom (from mockup home.html):

1. **Account banner** — avatar (uses existing `avatarInner()` helper), name+email via `applyProfile`
   data, tier pill (existing `tierBadge()`), inline stats: watch-hours-this-week, open requests,
   device count. Two buttons: "Security" (goto security tab) + "Your account" (goto account tab).
   Data: extend the existing `/api/stats` fetch usage; watch hours may be null -> show `—`.
2. **Continue hero** — biggest item from existing `/api/media/continue` (it already returns
   `{items:[{Id,Name,SeriesName,Type,UserData:{PlayedPercentage},BackdropImageTags}]}`-shaped data —
   check the actual shape in the file). Big backdrop `https://media.mysweetpea.cc/Items/{Id}/Images/Backdrop`
   with overlay gradient, title, resume button (opens `https://media.mysweetpea.cc`), progress bar.
   Hide section entirely if list empty.
3. **Your requests rail** — NEW. Fetch `/api/requests` (returns `{requests:[{tmdbId,mediaType,
   status,availability,title,createdAt}]}`). Render horizontal rail of journey cards:
   stage chip (REQUESTED if status=1 / APPROVED if status=2 / READY if availability>=4),
   title, 4-dot journey (dots filled by stage). Poster from
   `https://image.tmdb.org/p/w185{poster_path}` is NOT available (no poster path returned) —
   use a gradient placeholder tile with the title initial instead. Hide section if empty.
4. **Your services rail** — orb rail of the 9 SERVICES (existing SERVICES array in the file,
   with icons + status dots from existing `/api/status` fetch). Click = open service URL.
5. **Keep watching** — existing media/continue grid (already exists — restyle as Aurora rail,
   keep the same data + click behavior).
6. **Up next for you** — existing `/api/media/latest` grid (already exists — restyle).
7. **Your account rail** — 4 compact tiles linking to existing sections:
   Security checklist (goto security), Your devices (goto account), Recent activity (goto security),
   Watch anywhere (goto guides). These are navigation tiles, not new data.
8. **Recently added** — reuse latest-media items not already shown in Up next, NEW badge.

## HARD CONSTRAINTS (violating any = failed task)

1. **DO NOT touch**: the `<style>` token definitions (:root), `body::before` ambience,
   nav template (`top-nav`, `site-back`, `nav-id`, `acct-menu`/`acct-pop` dropdown and ALL
   `acct-*` elements/handlers), the palette/⌘K code, `updateFooterStatus`, `/auth/*` UI,
   security/account/stats/guides/contact/media panels' logic.
2. Only ADD new CSS classes (prefix `hm-`) in a clearly-marked `/* ---- aurora home ---- */`
   section at the END of the style block. Reuse existing vars (--v, --c, --card, --radius,
   --text-dim, --border, --e2, --t-fast...). No new colors outside the Aurora palette.
3. Only MODIFY: (a) the home panel's HTML inside the big template string,
   (b) the home-section render JS functions, (c) ADD new functions prefixed `hm`.
   Existing function names and IDs must keep working.
4. Media/requests images: Jellyfin images need no auth token in URL
   (`https://media.mysweetpea.cc/Items/{Id}/Images/Backdrop?fillWidth=1600&quality=70`).
5. Every fetch wrapped in try/catch with graceful empty-state (hide the section).
6. Mobile: rails scroll horizontally (`overflow-x:auto; scroll-snap-type:x mandatory`),
   banner + hero stack at ≤760px.
7. File must stay ONE file; keep `<!doctype html>` start and `</html>` end intact.
8. Temp/scratch files ONLY inside `.oc-tmp/` (repo dir). NEVER write outside the repo.
9. Do NOT run wrangler, git, or any deploy commands. Edit the file only.

## Verification (do all, then write `.oc-tmp/aurora-verification.md`)

1. `node --check` on every extracted <script> block (write them to .oc-tmp/*.js first).
2. brace balance of the full file.
3. Confirm every `/api/` route string in the file still present (compare list before/after).
4. Confirm `acct-trigger`, `acct-pop`, `updateFooterStatus`, `pal-open` handlers still present.
5. Write `.oc-tmp/aurora-verification.md` summarizing: checks + what you changed + line count delta.

Output when done: print "READY TO DEPLOY" plus a 10-line summary.
